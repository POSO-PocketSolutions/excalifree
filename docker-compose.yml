services:
  excalidraw-home:
    build: .
    container_name: excalidraw-home
    restart: unless-stopped
    environment:
      DATA_DIR: /data
      NODE_ENV: production
    ports:
      - "3030:3000"
    volumes:
      - excalidraw_data:/data

volumes:
  excalidraw_data:
