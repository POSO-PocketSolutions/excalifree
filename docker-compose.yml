services:
  poso-draw:
    build: .
    container_name: poso-draw
    restart: unless-stopped
    environment:
      DATA_DIR: /data
      NODE_ENV: production
    ports:
      - "3030:3000"
    volumes:
      - poso_draw_data:/data

volumes:
  poso_draw_data:
